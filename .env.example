# Lead delivery (Phase 1 of AGENTS.md). Both are required in production —
# the build refuses to start without them. See src/lib/env.ts.

DATABASE_URL=
RESEND_API_KEY=

# Optional overrides
RESEND_FROM="bpulse <intake@bpulse.dev>"
FOUNDER_EMAIL=contact@bpulse.dev
